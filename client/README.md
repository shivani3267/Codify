//Why Redux
token ko store cookie me kar he so, not in localStorage as can be fetched by js code by other  and not state as state varible val is lost when page/tab  closes

apne aap signup/login redux me user state store karte hai
